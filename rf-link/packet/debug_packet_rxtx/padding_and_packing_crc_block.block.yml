id: padding_and_packing_crc_block
label: Padding and Packing and CRC Block
category: '[Custom]'

inputs:
-   label: in
    domain: message
    dtype: message

outputs:
-   label: out
    domain: message
    dtype: message
-   label: precrc
    domain: message
    dtype: message
    optional: true

templates:
    imports: 'from padding_packing_crc_block import padding_packing_crc_block'
    make: 'padding_packing_crc_block()'

file_format: 1